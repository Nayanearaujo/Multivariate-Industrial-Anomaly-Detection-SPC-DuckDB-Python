# Power BI Handoff

This folder will contain the Power BI project after the processed analytical extracts are validated.

Planned inputs:

- `data/processed/fact_process_sample.parquet`
- `data/processed/fact_alert_event.parquet`
- `data/processed/fact_run_performance.parquet`
- `data/processed/dim_fault.parquet`
- `data/processed/dim_variable.parquet`

The dashboard layout and palette are documented in [`docs/DASHBOARD_SPEC.md`](../docs/DASHBOARD_SPEC.md).

